<nav class="navbar navbar-custom">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to("BillShare", root_path_url, class:"navbar-brand") %>
    </div>
    
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if account_signed_in? %>
        <ul class="nav navbar-nav">
          <li><%= link_to "Users", users_url %></li>
          <li><a href="#">Reports</a></li>
        </ul>
      <% end %>
      
      <ul class="nav navbar-nav navbar-right">
        <% if account_signed_in? %>
          <li><a href="#">Settings</a></li>
          <li><%= link_to "Logout", destroy_account_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Register", new_account_registration_url %></li>
          <li><%= link_to "Login", new_account_session_url %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>